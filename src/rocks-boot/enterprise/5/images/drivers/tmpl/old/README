Older driver update disks were built without using the RHEL5-style kmod RPM
packages. At build time (note that this does not affect runtime), ddiskit
will use the new format if an "rpm" directory exists, the old otherwise.
